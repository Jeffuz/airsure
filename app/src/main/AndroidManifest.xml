<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.camera" />
    <application
        android:configChanges="orientation|screenSize"
        android:extractNativeLibs="true"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false"/>

        <!-- For GPUv2 delegate -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false"/>

        <activity
            android:name="com.example.efficientdet_lite.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.EfficientDETLite">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.qualcomm.qti.objectdetection.CountrySelectionActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name="com.qualcomm.qti.objectdetection.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="unspecified">
        </activity>
    </application>

</manifest>
